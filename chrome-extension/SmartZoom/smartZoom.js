$(document).ready(function () {

  // chrome.extension.onRequest.addListener(function (request, sender, sendResponse) {
  //   switch (request.message) {
  //     case 'setText':
  //       window.selectText = request.data
  //       break;
  //   }
  // });

  // var startClick = function (info, tab) {
  //   chrome.tabs.executeScript({
  //     code: "getSelectionText();"
  //   }, function (selection) {
  //     alertRegexBoard();
  //   });
  // };

  // var alertRegexBoard = function () {
  //   chrome.tabs.executeScript({
  //     code: "popUpRegexBoard();"
  //   });
  // };

  // chrome.contextMenus.create({ "title": "Regex!!", "contexts": ["selection"], "onclick": startClick });
});
